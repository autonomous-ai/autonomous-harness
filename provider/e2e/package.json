{
  "name": "@autonomous/provider-e2e",
  "version": "0.3.0",
  "private": true,
  "description": "Runs the conformance suite against BOTH reference implementations, and asserts the flow properties a single-clause check cannot see.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3",
    "vitest": "^4.1.10"
  }
}
