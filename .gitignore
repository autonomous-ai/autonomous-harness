node_modules/
dist/
.tsbuildinfo
.DS_Store

# Per-deployment state, never source:
#   agents.json   names real directories on the machine it runs on
#   sessions.json maps A2A contextIds to Claude session ids
example-provider/agents.json
example-provider/sessions.json
